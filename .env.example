# 前端环境变量示例
# 复制为 .env 或在 Cloudflare Pages 项目设置里配置

# 后端 API 地址（Socket.io 会连到这个 origin）
#
# - 本地开发（npm run dev）：留空，vite proxy 会代理到 localhost:3001
# - CF Pages 部署：填你的 Cloudflare Tunnel 公网地址
#   例如：VITE_API_BASE=https://xxx-yyy-zzz.trycloudflare.com
#
# 注意：每次本地启动「在线游玩.bat」生成的隧道地址会变，
# 需要更新此变量后重新触发 Pages 构建。
VITE_API_BASE=
